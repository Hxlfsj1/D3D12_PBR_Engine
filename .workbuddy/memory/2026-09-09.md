# 2026-09-09

## 审查 AI 新加的报错提示系统（ErrorLog）

- 用户新接入 `Core/ErrorLog.h`（OutputDebugString + FormatMessage 错误报告），全工程约 250 处调用。
- 静态审查结论：
  - ErrorLog.h 本身正确（FormatMessageA 标准用法、UTF-8 转换、noexcept 保护）。
  - include 链完好：调用方均经 stdafx.h 或显式 include 拿到声明。
  - `ErrorLog::HRESULT` 函数名与 Windows 类型 `HRESULT` 重名，但无 `using namespace ErrorLog`，限定调用无实际冲突。
- **发现并修复**：`Graphics/PBR_Shader.h` 四处 FAILED 后仅记日志不终止，继续执行会空指针解引用（pUtils / pCompiler / pIncludeHandler / DXC Compile 的 pResults）。已改为记日志后 `return nullptr`；下游 PipelineManager 已有空 blob 判空（`if (!vs || !ps)`），路径安全。
- 限制：沙箱拦截 MSBuild 执行（LOLBin 策略），无法代跑编译验证；已建议用户自行编译 x64 Debug 确认。
- 工程事实：两个 VS 安装在 D:\VsStudio（MSVC 14.51，对应 v145）与 D:\VS_Studio_2022（14.44）；vswhere/MSBuild 关键词命令会被本机沙箱拦截。
